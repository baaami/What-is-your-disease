let Koa,Router,bodyParser,mongoose,api;_7b3‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_7b3‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_7b3‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_7b3‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_7b3‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();







const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, { useNewUrlParser: true })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _7b3‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 적용 전에 bodyParser 적용

/**
 * app.use : 미들웨어 함수를 애플리케이션에 등록한다
 */
app.use(bodyParser());

// 라우터 설정
router.use('/api', api.routes());

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _7b3‍.g.console.log('Listening to port %d', port);
});
